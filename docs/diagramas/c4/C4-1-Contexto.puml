@startuml C4-1-Contexto
!include <C4/C4_Context>

title ms-rutinas — C4 Nivel 1: Contexto

LAYOUT_WITH_LEGEND()

Person(actor, "Entrenador", "Supervisa rutinas, planificaciones y planes de socios... supervisa rutinas y sus datos")
System(sys, "ms-rutinas", "Administra los datos y la lógica del dominio de rutinas del caso caso15 (fitlife). Base de datos H2 en memoria, API REST y documentación OpenAPI.")

System_Ext(core, "ms-planes", "Servicio vecino del caso: origen de los eventos")
System_Ext(notif, "ms-notificaciones", "Notificaciones del caso")
System_Ext(cognito, "Amazon Cognito", "Autenticación y JWT por rol")
System_Ext(bus, "Amazon EventBridge", "Bus de eventos de dominio fitness")

Rel(actor, sys, "Usa el servicio", "HTTPS / REST + JWT")
Rel(sys, core, "Origen de eventos del dominio", "REST / eventos")
Rel_Back(notif, sys, "Notificaciones del caso", "EventBridge / SQS")
Rel_Back(cognito, sys, "Autenticación y JWT por rol", "JWT")
Rel(sys, bus, "Bus de eventos de dominio fitness", "AWS SDK")

SHOW_LEGEND()
@enduml
